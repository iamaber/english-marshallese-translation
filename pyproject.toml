[project]
name = "english-marshallese-translation"
version = "0.1.0"
description = "English to Marshallese translator using GPT-5"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1.0.0",
    "openai>=1.0.0",
]
